# Base URL for the avtopulse-backend Go service. Leave empty — the Vite dev
# server proxies /api to http://localhost:8090 (see vite.config.ts), and in
# production /api is proxied through the same origin by Caddy.
VITE_AUKSION_API_BASE=
